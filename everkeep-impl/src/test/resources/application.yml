spring:
  datasource:
    url: jdbc:tc:postgresql://localhost:32770/everkeep
    username: everkeep
    password: everkeep
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  security:
    basic:
      enabled: false